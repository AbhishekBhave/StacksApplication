---
phase: 2
plan: 05
type: execute
wave: 2
depends_on: [03, 04]
files_modified:
  - expo/scripts/check-env.js
  - README.md
  - expo/lib/logger.ts
autonomous: true
requirements: [SECU-02]
---

# Plan 05 — Client logging hygiene and secret grep gates

<objective>
Ensure **SECU-02**: client logging and local dev scripts **avoid or redact** Plaid tokens, `public_token`, `link_token`, and raw account numbers; extend env checks so forbidden secret names cannot ship in `expo/.env` for this phase.
</objective>

<threat_model>
**ASVS L1 alignment**

| Threat | Mitigation in this plan |
|--------|-------------------------|
| T-2-40 Accidental PII in Metro logs | Provide `expo/lib/logger.ts` with `safeLog` that strips known keys (`access_token`, `public_token`, `link_token`, `account_id`) before `console.log` in dev only. |
| T-2-41 Secret in client env file | `check-env.js` rejects lines where key matches `/PLAID_SECRET/i`, `/SUPABASE_SERVICE_ROLE/i`, `/OPENAI_API_KEY/i` inside `expo/.env`. |
</threat_model>

<tasks>

<task id="2-05-01" type="execute">
<read_first>
- `expo/scripts/check-env.js`
- `.planning/phases/02-plaid-connection-and-transaction-pipeline/02-CONTEXT.md`
</read_first>
<action>
Update `expo/scripts/check-env.js` to **fail** (non-zero exit) if `expo/.env` contains any of these **key names** (case-insensitive, ignoring leading `EXPO_PUBLIC_` for the secret ones where applicable):

- `PLAID_SECRET`
- `PLAID_ACCESS_TOKEN` (client must never hold)
- `SUPABASE_SERVICE_ROLE_KEY`
- `OPENAI_API_KEY`

Allowed: `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY` only from existing Phase 1 checks.

Add `expo/lib/logger.ts` exporting `devLog(...args)` that in `__DEV__` runs `console.log` with shallow object redaction: replace values whose key matches `/token|secret|password|access_token|public_token/i` with `[REDACTED]`.

Replace stray `console.log` in new Plaid-related files (from Plans 03–04) with `devLog` where logs are still needed.

Append `README.md` subsection **Security — Plaid prototype** stating: Plaid secrets live only in Supabase Edge secrets; never add `PLAID_SECRET` to `expo/.env`.
</action>
<acceptance_criteria>
- `expo/scripts/check-env.js` contains literal substring `PLAID_SECRET`
- `expo/lib/logger.ts` exists and contains `[REDACTED]`
- `README.md` contains literal substring `Plaid secrets`
</acceptance_criteria>
<verify>cd /Users/abhishekbhave/Desktop/FEH/prototype && grep -q PLAID_SECRET expo/scripts/check-env.js && test -f expo/lib/logger.ts</verify>
</task>

</tasks>

<verification>
1. `node expo/scripts/check-env.js` exits 0 with a clean `.env` missing forbidden keys.
2. Create temp `.env` line `PLAID_SECRET=x` and confirm script exits non-zero (document in summary optional).
</verification>

<success_criteria>
Client-side logging and env validation align with SECU-02 expectations for the Plaid phase.
</success_criteria>

<must_haves>
- Hard reject forbidden env keys in `check-env.js`
- Central dev logger with redaction helper
</must_haves>
