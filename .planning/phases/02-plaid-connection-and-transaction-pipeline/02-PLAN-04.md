---
phase: 2
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - expo/app/(app)/transactions.tsx
  - expo/components/TransactionList.tsx
  - expo/lib/transactions.ts
  - expo/app/(app)/bank.tsx
autonomous: true
requirements: [BANK-02, BANK-03, BANK-04]
---

# Plan 04 — Transactions UI, refresh, disconnect

<objective>
Ship the **transaction list** experience per `02-CONTEXT.md` / `02-UI-SPEC.md`: flat newest-first list, skeleton + loading copy, dismissible success banner, pull-to-refresh calling `plaid-sync`, last-updated header, inline error banner, empty “still fetching” state, and **Disconnect** flow calling `plaid-disconnect` with confirmation.
</objective>

<threat_model>
**ASVS L1 alignment**

| Threat | Mitigation in this plan |
|--------|-------------------------|
| T-2-30 XSS via merchant text | React Native `Text` escapes by default — do not use `dangerouslySetInnerHTML` (N/A); still avoid `WebView` with unsanitized HTML. |
| T-2-31 Stale data after disconnect | On successful disconnect, clear React Query / local state and `router.replace('/(app)/home')` or bank screen showing unlinked. |
</threat_model>

<tasks>

<task id="2-04-01" type="execute">
<read_first>
- `.planning/phases/02-plaid-connection-and-transaction-pipeline/02-UI-SPEC.md`
- `expo/lib/supabase.ts`
- `supabase/migrations/20260418120000_phase1_core.sql`
</read_first>
<action>
Add `expo/lib/transactions.ts` with:
- `fetchTransactionsForUser():` `supabase.from('transactions').select('id, amount, posted_date, raw_name, merchant_name, created_at').eq('user_id', sessionUser.id).order('posted_date', { ascending: false }).order('created_at', { ascending: false })` — adjust column names to match migration; **filter** `pending eq false` in query if column exists.

Implement `expo/components/TransactionList.tsx`:
- `FlatList` data from props; **RefreshControl** only (pull-to-refresh) — call `supabase.functions.invoke('plaid-sync')` then refetch table.
- **Skeleton** placeholder rows (3–6) when `loading && data.length===0`.
- Subtitle text region showing **Loading transactions…** when appropriate.
- **Dismissible banner** when route param `justLinked` or similar flag set.
- **Inline error banner** component at top with dismiss button; show on sync function error.

Implement `expo/app/(app)/transactions.tsx`:
- Load session; subscribe or focus refresh.
- Header area showing **Last updated** using `bank_links.last_successful_sync_at` formatted with `Intl.RelativeTimeFormat` or simple minutes-ago string.
- Navigate here as default post-link (already from Plan 03 — align param handling).

Extend `expo/app/(app)/bank.tsx`:
- When linked: show institution + mask; **Disconnect bank** button opens confirm Alert; on confirm call `plaid-disconnect` then clear cache and navigate home.
- **Replace bank** secondary action: call disconnect flow (or dedicated endpoint) then route to `pre-plaid` — match CONTEXT (revoke then Link again).

**Pending:** ensure list query uses `.eq('pending', false)` or equivalent so pending rows never render.

Link errors from Plan 03: if not already, ensure **modal** presentation for Link SDK fatal errors per UI-SPEC.
</action>
<acceptance_criteria>
- `expo/app/(app)/transactions.tsx` exists
- `expo/components/TransactionList.tsx` exists and contains `RefreshControl`
- `expo/lib/transactions.ts` exists and contains substring `from('transactions')`
- `expo/app/(app)/bank.tsx` contains substring `Disconnect` (case-sensitive) OR `disconnect`
- `expo/app/(app)/transactions.tsx` contains substring `Loading transactions` OR `Loading transactions…`
</acceptance_criteria>
<verify>cd /Users/abhishekbhave/Desktop/FEH/prototype && test -f expo/app/\(app\)/transactions.tsx && grep -q RefreshControl expo/components/TransactionList.tsx</verify>
</task>

</tasks>

<verification>
1. Pull-to-refresh invokes `plaid-sync` (grep `plaid-sync` in `expo/`).
2. Disconnect invokes `plaid-disconnect` (grep in `expo/`).
3. Jest still passes: `cd expo && npm test -- --ci`.
</verification>

<success_criteria>
Roadmap items for visible transactions, manual refresh, and normalized storage read path are satisfied from the user’s perspective.
</success_criteria>

<must_haves>
- Pull-to-refresh only (no standalone refresh button)
- Dismissible success + error banners per CONTEXT
- Disconnect + delete semantics triggered from UI
</must_haves>
