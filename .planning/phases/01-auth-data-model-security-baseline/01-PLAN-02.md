---
phase: 1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260418120000_phase1_core.sql
  - supabase/config.toml
autonomous: false
requirements: [AUTH-04, SECU-03]
---

# Plan 02 — Postgres schema, RLS, and schema push

<objective>
Add Supabase migration defining `profiles`, `bank_links`, `transactions`, `budgets`, `goals`, `insights` with `user_id` ownership and strict RLS; enforce at most one active bank link per user; run **[BLOCKING]** `supabase db push` after migration files are final.
</objective>

<threat_model>
**ASVS L1 alignment**

| Threat | Mitigation |
|--------|------------|
| T-2-01 IDOR / cross-tenant reads | RLS `USING (auth.uid() = user_id)` on all user tables; `anon` has no grants on these tables. |
| T-2-02 Privilege escalation | No policies allowing `anon` INSERT/SELECT on finance tables; authenticated users only touch own rows. |
| T-2-03 Token exfil via RLS | Do not add columns for Plaid access tokens in this migration; if `bank_links` holds only non-secret metadata, document secrets for Phase 2 server-side only. |
</threat_model>

<tasks>

<task id="1-02-01" type="execute">
<read_first>
- `.planning/phases/01-auth-data-model-security-baseline/01-CONTEXT.md`
- `.planning/phases/01-auth-data-model-security-baseline/01-RESEARCH.md`
</read_first>
<action>
Initialize Supabase CLI layout at repo root if missing: `supabase init` (creates `supabase/config.toml`).

Create `supabase/migrations/20260418120000_phase1_core.sql` containing:

1. `profiles` table: `id uuid primary key references auth.users(id) on delete cascade`, `display_name text`, `created_at timestamptz default now()`.

2. `bank_links` table: `id uuid primary key default gen_random_uuid()`, `user_id uuid not null references auth.users(id) on delete cascade`, `status text not null check (status in ('active','revoked'))`, `created_at timestamptz default now()` — **no** secret token columns.

3. Partial unique index: `create unique index one_active_bank_link_per_user on bank_links (user_id) where status = 'active';`

4. Tables `transactions`, `budgets`, `goals`, `insights` each with: `id uuid primary key default gen_random_uuid()`, `user_id uuid not null references auth.users(id) on delete cascade`, `created_at timestamptz default now()` — add minimal placeholder columns as needed (`amount numeric`, `category text`, etc.) only if required for NOT NULL sanity; keep skeleton small.

5. For **each** table above: `alter table ... enable row level security;`

6. Policies (names explicit): for `profiles` — `select/update/insert` for `auth.uid() = id`; for `bank_links`, `transactions`, `budgets`, `goals`, `insights` — full CRUD where `auth.uid() = user_id` using separate `FOR SELECT`, `FOR INSERT`, `FOR UPDATE`, `FOR DELETE` policies as appropriate.

7. Trigger: on `auth.users` insert, insert matching `profiles` row with `display_name` default `''` or `split_part(email,'@',1)` via function `handle_new_user` — implement as standard Supabase pattern.

Use exact policy SQL in migration file (no “TODO” placeholders).
</action>
<acceptance_criteria>
- `supabase/migrations/20260418120000_phase1_core.sql` contains `enable row level security` at least 6 times (one per table)
- Same file contains `one_active_bank_link_per_user`
- Same file contains `handle_new_user` or `on auth.user created` trigger pattern for `profiles`
</acceptance_criteria>
<verify>grep -c "enable row level security" /Users/abhishekbhave/Desktop/FEH/prototype/supabase/migrations/20260418120000_phase1_core.sql 2>/dev/null || echo "0"</verify>
</task>

<task id="1-02-02" type="execute">
<read_first>
- `supabase/migrations/20260418120000_phase1_core.sql`
</read_first>
<action>
**[BLOCKING]** Apply migrations to the linked Supabase project:

Run from repo root: `supabase db push`  
Non-TTY: ensure `SUPABASE_ACCESS_TOKEN` is set if CLI requires it, or use `supabase link` interactively once (`autonomous: false`).

If push fails, fix SQL and re-run until success. Record project ref used in commit message body if needed (no secrets).
</action>
<acceptance_criteria>
- CLI command `supabase db push` completes with exit code 0 (executor documents output snippet in SUMMARY)
- Remote DB contains table `profiles` (verified via `supabase db diff` empty or dashboard)
</acceptance_criteria>
<verify>echo "Manual: confirm supabase db push exit 0 in SUMMARY.md"</verify>
</task>

</tasks>

<verification>
1. Migration file syntax valid (push succeeded).
2. `grep -q "row level security" supabase/migrations/*.sql`
3. No `plaid` token column strings in migration.
</verification>

<success_criteria>
All Phase 1 data tables exist remotely with RLS enforcing per-user access and single active bank link constraint.
</success_criteria>

<must_haves>
- RLS on every user data table listed in roadmap success criteria
- Schema applied to live database via push
- Owner-only write semantics
</must_haves>
