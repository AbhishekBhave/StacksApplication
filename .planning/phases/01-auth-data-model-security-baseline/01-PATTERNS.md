# Phase 1 — Pattern Map

**Status:** Greenfield — no application source files exist in the repository yet.

## Canonical References (analogs)

| Intended file / area | Analog in codebase | Notes |
|---------------------|-------------------|--------|
| Expo + Supabase client | — | Follow `.planning/research/STACK.md` and official Expo + Supabase guides |
| Navigation | — | Use `expo-router` file conventions; no prior `app/` tree |
| SQL / RLS | — | Follow `01-RESEARCH.md` policy patterns; migrations in `supabase/migrations/` |

## Conventions to Mirror

- **TypeScript** strict mode in new Expo project.
- **Environment:** only `EXPO_PUBLIC_*` for Supabase URL/anon key per PROJECT security constraints.

## PATTERN MAPPING COMPLETE
