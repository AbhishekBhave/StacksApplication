---
phase: 1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - expo/package.json
  - expo/app/_layout.tsx
  - expo/app/index.tsx
  - expo/lib/supabase.ts
  - expo/.env.example
  - expo/jest.config.js
  - expo/__tests__/smoke.test.ts
  - README.md
autonomous: true
requirements: [AUTH-02, SECU-04]
---

# Plan 01 — Expo scaffold and Supabase client

<objective>
Create the `expo/` React Native (Expo) TypeScript app with `expo-router`, wire `@supabase/supabase-js` with Expo-supported session persistence, and add Jest smoke test so validation commands exist.
</objective>

<threat_model>
**ASVS L1 alignment**

| Threat | Mitigation in this plan |
|--------|-------------------------|
| T-1-01 Credential exposure | Only `EXPO_PUBLIC_SUPABASE_URL` and `EXPO_PUBLIC_SUPABASE_ANON_KEY` in `.env.example` — never service_role. |
| T-1-02 Insecure session storage | Use Supabase documented Expo persistence (SecureStore or AsyncStorage per official recipe); document choice in `README.md`. |
| T-1-03 Missing transport security | Rely on HTTPS Supabase endpoints only (enforce URL scheme `https://` in client bootstrap assert or comment). |
</threat_model>

<tasks>

<task id="1-01-01" type="execute">
<read_first>
- `.planning/phases/01-auth-data-model-security-baseline/01-CONTEXT.md`
- `.planning/research/STACK.md`
</read_first>
<action>
From repository root `/Users/abhishekbhave/Desktop/FEH/prototype`, run `npx create-expo-app@latest expo --template tabs` (or equivalent Expo SDK 52+ tabs + TypeScript template). If `expo/` exists, skip create and only add missing deps.

Install: `@supabase/supabase-js`, `@react-native-async-storage/async-storage` (if required by chosen Supabase persistence recipe), `jest-expo`, `jest`, `@types/jest`.

Create `expo/lib/supabase.ts` exporting `createClient(process.env.EXPO_PUBLIC_SUPABASE_URL!, process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!, { auth: { storage: AsyncStorage, persistSession: true, autoRefreshToken: true } })` — adjust to match official Supabase Expo guide exactly at implementation time.

Create `expo/.env.example` with literal lines:
```
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

Add `README.md` at repo root (or append if absent) sections: **Prerequisites**, **Setup** (`cd expo && npm install`, copy `.env.example` to `.env`), **Run** (`npx expo start`).
</action>
<acceptance_criteria>
- `expo/package.json` contains dependency `"@supabase/supabase-js"`
- `expo/lib/supabase.ts` contains `createClient`
- `expo/.env.example` contains both `EXPO_PUBLIC_SUPABASE_URL=` and `EXPO_PUBLIC_SUPABASE_ANON_KEY=`
- `README.md` contains the substring `cd expo`
</acceptance_criteria>
<verify>cd /Users/abhishekbhave/Desktop/FEH/prototype && test -f expo/lib/supabase.ts && grep -q createClient expo/lib/supabase.ts && grep -q EXPO_PUBLIC_SUPABASE_URL expo/.env.example</verify>
</task>

<task id="1-01-02" type="execute">
<read_first>
- `expo/package.json`
</read_first>
<action>
Add `expo/jest.config.js` using `jest-expo` preset (copy from current jest-expo docs).

Add `expo/__tests__/smoke.test.ts` with one test: `expect(1 + 1).toBe(2)`.

Add to `expo/package.json` scripts: `"test": "jest"`.
</action>
<acceptance_criteria>
- `expo/jest.config.js` exists
- `expo/package.json` contains `"test": "jest"`
- `npm test` in `expo/` exits 0
</acceptance_criteria>
<verify>cd /Users/abhishekbhave/Desktop/FEH/prototype/expo && npm test -- --ci 2>/dev/null | tail -1</verify>
</task>

</tasks>

<verification>
1. `cd expo && npm test -- --ci` exits 0.
2. `grep -r "createClient" expo/lib/supabase.ts` finds implementation.
3. No file under `expo/` contains the string `service_role`.
</verification>

<success_criteria>
Expo app directory exists with Supabase client module, env template, and passing Jest smoke test.
</success_criteria>

<must_haves>
- Session-capable Supabase client for mobile
- Public anon key only in client config
- Automated test command for Phase 1 validation strategy
</must_haves>
